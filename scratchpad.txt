Fix primitive obsession

Consider what to do about withdrawing more than the starting balance -- assert that this is impossible?

Desk check - spreadsheet results compared to StockMarketYear results

Assert on TaxRate--need to enforce legal values

Take .value() out of nextYear() test