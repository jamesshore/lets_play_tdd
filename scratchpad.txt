'year' concept should be object?
fail fast in StockMarketTableModel.getValueAt() default case
Polish UI look & feel
Pull out StockMarketAccount from StockMarketTableModel?
Create packages
How does StockMarket know which year is which?
fail fast in StockMarket.getYear() when request year is out of range
StockMarketTabelModel shouldn't know about startingYear and endingYear--just the StockMarket should

---

Assert on TaxRate--need to enforce legal values

Desk check - spreadsheet results compared to StockMarketYear results

---
UI thingies to investigate further:

Desktop
- Swing (MigLayout? JGoodies FormLayout? Macify?)
- SwiXml (layer on top of Swing)
